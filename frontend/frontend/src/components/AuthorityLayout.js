// src/components/AuthorityLayout.js
import React from "react";

const AuthorityLayout = ({ children }) => (
  <div className="authority-layout">
    {/* add header/nav/footer if needed */}
    {children}
  </div>
);

export default AuthorityLayout;
